<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="astro.grpc.server.dao.MariaDAO">
    <resultMap id="result" type="astro.grpc.server.domain.AstroMessage">
        <result property="uuid" column="uuid"/>
        <result property="datetime" column="datetime"/>
        <result property="index" column="index"/>
        <result property="topic" column="topic"/>
        <result property="message" column="message"/>
    </resultMap>

    <!--<select id="createDB" parameterType="string">-->
        <!--CREATE DATABASE #{dataBase};-->
    <!--</select>-->

    <!--<select id="createTable" parameterType="string">-->
        <!--CREATE TABLE #{table} (-->
            <!--uuid char(50) NOT NULL,-->
            <!--datetime datetime NOT NULL,-->
            <!--index int(11) NOT NULL,-->
            <!--topic char(50) NOT NULL,-->
            <!--message longtext NOT NULL,-->
            <!--PRIMARY KEY(uuid)-->
        <!--);-->
    <!--</select>-->
    <!---->
    <insert id="insert" parameterType="astro.grpc.server.domain.AstroMessage"  useGeneratedKeys="true" keyProperty="list_id">
        INSERT INTO mybatis_test VALUES (#{uuid}, #{datetime}, #{index}, #{topic}, #{message});
    </insert>
</mapper>